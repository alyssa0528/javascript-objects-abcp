var playlist = {BSB: 'I Want It That Way'};

function updatePlaylist(playlist, artistName, songTitle) {
  Object.assign(playlist, {artistName: 'songTitle'})
  return playlist;
}

function removeFromPlaylist(playlist, artistName) {
  
  return playlist;
}